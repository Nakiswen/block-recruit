{
  "name": "block-recruit",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@headlessui/react": "^1.7.18",
    "bufferutil": "^4.0.9",
    "next": "^13.4.1",
    "utf-8-validate": "^6.0.5"
  }
}